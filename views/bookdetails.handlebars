<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Details</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <nav class="navbar">
        <div class="logo">
            <!-- Add your logo/image here -->
        </div>
        <ul class="navbar-links">
            <li class="navbar-link"><a href="/">Home</a></li>
            <li class="navbar-link"><a href="/reviews">Reviews</a></li>
            {{#if isAuthenticated}}
            <li class="navbar-link"><a href="/user/reviews">User Reviews</a></li>
            <li class="navbar-link"><a href="/logout">Logout</a></li>
            {{else}}
            <li class="navbar-link"><a href="/login">Login</a></li>
            <li class="navbar-link"><a href="/signup">Signup</a></li>
            {{/if}}
        </ul>
    </nav>

    <section class="book-details">
        <div class="container">
            <h1 class="title">Book Details</h1>

            <div class="book-details-container">
                <h2 class="book-title">{{book.title}}</h2>
                <p class="book-author">Author: {{book.author}}</p>
                <p class="book-published-date">Published Date: {{book.published_date}}</p>
                <p class="book-genre">Genre: {{book.genre}}</p>
                <p class="book-description">Description: {{book.description}}</p>
                <p class="book-reviews">Reviews: {{book.reviews}}</p>
            </div>
        </div>
    </section>

    <script src="bookdetails.js"></script>
</body>
</html>
